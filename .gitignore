Python

pycache/
*.pyc
*.pyo
*.pyd
.venv/
venv/Lib/
env/
.idea/

Operating System Files

.DS_Store
Thumbs.db

TensorFlow / Keras Artifacts

These files are too large (over 100MB) and should be generated, not tracked.

The user can retrain the model to generate these.

covid_detection_model.h5
covid_detection_model.keras

Logs and Data

*.log
data/
datasets/

# Ignore Python virtual environment
venv/
# Ignore Keras / TensorFlow model files
*.h5
*.keras
# If using Git LFS, you can later track model files with .gitattributes
# e.g. add a .gitattributes entry: "*.h5 filter=lfs diff=lfs merge=lfs -text"